en-US:
  settings:

    repositories:
      label: 'Repositories'
      groups:
        repo_git:
          label: 'Git Repository Management'
          settings:
            repo:
              label: 'Repository'
              description: 'The repository containing the account information. (default: gitolite-admin)'