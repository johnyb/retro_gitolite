<% @page_title = _('Edit SSH Public Key') -%>

<%= render :partial => '/admin/shared/edit_form', :locals => {
      :record => @ssh_pubkey,
      :member_path => admin_user_ssh_pubkey_path,
      :collection_path => admin_user_ssh_pubkey_path } %>
